<?xml version="1.0"?>
<ruleset name="Test">
    <description>Sample Coding Standards</description>

    <!-- Scan only PHP files -->
    <arg name="extensions" value="php"/>

    <!-- Show sniff codes in all reports -->
    <arg value="ns"/>

    <!-- Use the PSR-12 standard. -->
    <rule ref="PSR12">
    </rule>
</ruleset>